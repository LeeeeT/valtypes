name: Create release

on:
  push:
    branches: [main]

jobs:
  create-release:
    runs-on: ubuntu-latest
    steps:
      - uses: google-github-actions/release-please-action@main
        with:
          token: ${{ secrets.PAT }}
          release-type: python
          package-name: valtypes
